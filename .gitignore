# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*~

# Nothing build-related to ignore — this is a static site with no build step.

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
